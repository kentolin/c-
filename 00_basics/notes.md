### 00_BASICS Notes

✅ Learned:

- Every program starts with `main()`.
- `#include <iostream>` is needed for input/output.
- Variables must have a type.
- Division of int/int gives int.
- "endl" flushes the output buffer.

💡 Tip:
Use `auto` for automatic type deduction (C++11+).
