### 02_FUNCTIONS Notes

✅ Learned:

- Function = named reusable block of code.
- Syntax: returnType functionName(parameters) { ... }
- Can pass values (arguments) and get results (return).
- Overloading allows same name for different parameter types.
- Recursion = self-calling functions with base condition.

💡 Tips:

- Keep functions small and single-purpose.
- Prefer return values over global variables.
- Debug recursive functions by printing current step.

🧩 Practice:

1. Function to check if a number is even/odd.
2. Function to return maximum of 3 numbers.
3. Recursive function for Fibonacci series.
4. Overload “print” for int, double, and string.
