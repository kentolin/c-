### 03_OOP Notes

✅ Learned:

- **Class & Object** → blueprints and instances.
- **Constructor** → automatic initialization.
- **Encapsulation** → protect data using private/public.
- **Inheritance** → code reuse and hierarchy.
- **Polymorphism** → dynamic method overriding via virtual functions.

💡 Tips:

- Always initialize members (constructors or initializer lists).
- Use `const` or references for read-only members.
- Prefer smart pointers (`unique_ptr`) to avoid manual `delete`.

🧩 Practice:

1. Class `Student` with private `marks` and methods to calculate grade.
2. Class `Shape` with derived `Circle`, `Rectangle` using inheritance.
3. Virtual method `draw()` to demonstrate polymorphism.
4. Add constructor/destructor messages to understand object lifetime.
