### 06_CPP17_20_23 Notes

✅ Learned:

- **Structured Bindings** → clean unpacking.
- **std::optional / std::variant** → safe type/value handling.
- **Concepts** → constrained, readable templates.
- **Ranges** → functional-style data pipelines.
- **Coroutines** → asynchronous code without threads.
- **std::expected / std::print** → cleaner error and output.

💡 Takeaway:
Modern C++ = expressive, type-safe, and clean.
Write intent, not implementation details.

🧩 Practice:

1. Function returning optional<string> if input is valid.
2. Template with concept enforcing arithmetic types.
3. Range pipeline: filter odd numbers, multiply by 3.
4. Expected-based safe division calculator.
